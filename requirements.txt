# ChessPlayer v3 — Python dependencies
# Pinned to latest stable compatible versions as of 2025.
# Tested on Python 3.11+ and 3.12+.
# Zero new dependencies for the coach backend (uses stdlib + python-chess + sqlite3).

# GUI framework
PySide6>=6.8,<6.9

# Chess logic — move generation, PGN parsing, FEN, UCI
python-chess>=1.11,<2.0

# Config file parsing
PyYAML>=6.0.2,<7.0
